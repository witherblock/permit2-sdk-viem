{
  "name": "permit2-sdk-viem",
  "version": "0.0.1",
  "description": "An sdk for interacting with permit2 via viem",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.cjs.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "test": "jest",
    "build": "node scripts/build.js && tsc --emitDeclarationOnly --outDir dist",
    "interop": "yarn node scripts/writeInterop.js",
    "prepare": "yarn build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/witherblock/permit2-sdk-viem.git"
  },
  "license": "MIT",
  "peerDependencies": {
    "viem": "^2"
  },
  "dependencies": {
    "tiny-invariant": "^1.3.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "esbuild": "^0.23.1",
    "jest": "^29.5.0",
    "prettier": "^3.3.3",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3",
    "viem": "^2"
  },
  "prettier": {
    "printWidth": 120,
    "semi": false,
    "singleQuote": true
  }
}
